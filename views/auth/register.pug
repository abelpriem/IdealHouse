extends ../layout/index

block container
    div.py-10
        h1.text-4xl.my-10.font-extrabold.text-center Ideal 
            span.font-normal House
        h2.text-center.text-2xl.font-extrabold Crear una cuenta

        if errors 
            div(class="max-w-md mx-auto my-10")
                each error in errors 
                    p(class="bg-red-600 text-white uppercase text-xs text-center p-2 mb-1 font-bold")= error

        div.mt-8.mx-auto.max-w-md
            div.bg-white.py-8.px-4.shadow
                form.space-y-4(method="POST" action="/auth/register")
                    input(type="hidden" name="_csrf" value= csrfToken)
                    div
                        label.block.text-sm.uppercase.text-gray-500.mb-2.font-bold(for="name") Nombre
                        input#name.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Tu nombre" type="text" name="name" value= user ? user.name : '')
                    div
                        label.block.text-sm.uppercase.text-gray-500.mb-2.font-bold(for="email") Email
                        input#email.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Dinos tu email" type="text" name="email" value= user ? user.email : '' required)
                    div
                        label.block.text-sm.uppercase.text-gray-500.mb-2.font-bold(for="password") Contraseña
                        input#password.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Introduce tu contraseña" type="password" name="password" value= user ? user.password : '')
                    div
                        label.block.text-sm.uppercase.text-gray-500.mb-2.font-bold(for="repeat_password") Repetir contraseña
                        input#repeat_password.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Repite tu contraseña" type="password" name="repeat_password" value= user ? user.repeat_password : '')

                    div.flex.items-center.justify-center.text-gray-500.text-xs
                        a(href="/auth/login") ¿Ya tienes una cuenta con nosotros? Iniciar sesión

                    input(class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 cursor-pointer" type="submit" value="Crear ahora!")